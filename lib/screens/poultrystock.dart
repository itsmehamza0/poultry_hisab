import 'package:flutter/material.dart';

class PoultryStockScreen extends StatelessWidget {
  const PoultryStockScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("PoultryStockScreen")),
      body: Center(child: Text("Poultry Stock Screen")),
    );
  }
}
